version: '2'

services:
  web:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ../:/code
      - ./site.conf:/etc/nginx/conf.d/default.conf
    links:
      - php
  php:
    build: ./php
    expose:
        - '9000'
    volumes:
        - ../:/code

